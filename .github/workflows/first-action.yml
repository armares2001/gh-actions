name: First Workflow
on: workflow_dispatch

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "Hello World"
  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "Hello World 2"
